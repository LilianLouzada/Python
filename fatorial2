import sys
a = int(input('Insira aqui a quantidade de números que vão existir no conjunto:'))
conj = 0
soma = 0
numero = []
while a > conj:
    b = int(input('Digite o número:'))
    if b > 1000 or b < 0:
        print('Não irei calcular')
        sys.exit()
    conj = conj + 1
    soma = b + soma
    numero.append(b)
print('Maior número:', max(numero))
print('Menor número:', min(numero))
print(f'O valor da soma dos números digitados é:{soma}')
